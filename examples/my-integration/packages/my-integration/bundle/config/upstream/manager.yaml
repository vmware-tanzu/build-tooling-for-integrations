apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-integration-manager
  labels:
    app: my-integration-manager
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-integration-manager
  template:
    metadata:
      labels:
        app: my-integration-manager
    spec:
      containers:
        - name: manager
          image: my-integration-manager:latest
          ports:
            - containerPort: 80
